{
}
/* 매핑된 객체 타입 */
// 기존의 다른 타입으로부터 새로운 객체 속성을 만들어내는 타입을 의미
// 인터페이스에서는 쓰지 못하고, 타입 별칭에서만 사용할 수 있다.
{
}
{
}
{
    const copyTuple = [1, 2, 3];
}
{
    const copyArr = [1, 3, 9];
}
// 속성 이름 바꾸기
// Capitalize 는 타입스크립트에서 제공하는 타입으로 문자열의 첫 번째 자리를 대문자화한다.
// as 예약어를 통해 속성 이름을 어떻게 바꿀지 정할 수 있다.
{
    /*
          type Copy = {
      Name: string;
      Age: number;
      Married: boolean;
  }
          */
}
export {};
